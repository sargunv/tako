[package]
name = "tako-dbus"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "D-Bus server + client (zbus), session-bus service."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
