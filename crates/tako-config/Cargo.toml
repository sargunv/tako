[package]
name = "tako-config"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "KConfig bridge + ghostty config reader + project JSON."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
