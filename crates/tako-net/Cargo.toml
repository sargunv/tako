[package]
name = "tako-net"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "procfs port scanner + per-workspace attribution."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
