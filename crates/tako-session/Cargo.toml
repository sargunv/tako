[package]
name = "tako-session"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "serde snapshot to ~/.local/state/tako/ (XDG paths)."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
