[package]
name = "tako-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "`tako` CLI (clap + serde_json) -> D-Bus."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
