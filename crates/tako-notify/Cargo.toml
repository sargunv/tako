[package]
name = "tako-notify"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OSC ingest + notification store + KNotification bridge."

[lib]
path = "src/lib.rs"

[lints]
workspace = true
